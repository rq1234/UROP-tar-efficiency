%%In this script we use an alternative methodology
%We run a dummy variable regression where the dummies indicate the two
%non-efficient states; instead of running the normal regression though we
%just run yt - y(t-1) = dummy1*state1 + dummy2*state2 without the constant 