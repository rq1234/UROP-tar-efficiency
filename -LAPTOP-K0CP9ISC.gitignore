venv/
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
.env

# Rebuilt analysis outputs are regenerated by scripts/run_all.py.
# They are tracked deliberately (see CLAUDE.md) — do NOT ignore:
#   outputs/rebuilt/
# data/ and results/ ARE tracked: they are the paper's vintage ground truth.
